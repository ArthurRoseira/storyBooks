<H6>Dashboard</H6>
<h3>Welcome {{name}}</h3>
<p>Here are your Stories</p>
{{#if stories}}
<table class="striped">
 <thead>
  <tr>
   <th>Title</th>
   <th>Date</th>
   <th>Status</th>
   <th></th>
  </tr>
 </thead>
 <tbody>
  {{#each stories}}
  <tr>
   <td><a href="/stories/{{_id}}"></a>{{title}}</td>
   <td>{{formatDate createdAt 'MMMM Do YYYY, h:mm:s a'}}</td>
   <td><span class="dash-status">{{status}}</span></td>
  </tr>
  {{/each}}
 </tbody>
</table>
{{else}}
<p>You have no Stories Yet, Wanna Give a Try?</p>
{{/if}}

{{!-- thead tag is used within a table to group content in one line --}}